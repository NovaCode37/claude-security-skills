[pytest]
# Each skill keeps its engine next to its tests; tests add their own dir to
# sys.path, so we just point pytest at the skills tree.
testpaths = skills
python_files = test_*.py
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
